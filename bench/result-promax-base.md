# 鸿蒙五套对话预设「六边形」跑分报告

> 跑分引擎：能力题走 opencode-go API（模型 deepseek-v4-flash，免费 cost:0）；缓存命中走 DeepSeek 直连 API（同模型，准确）。日期 2026-08-18。六边形 = 数学/代码/逻辑/事实/规划/自我修正 6 轴 × 2 题，每轴 0-100；性能表 = 缓存命中率 + 输出效率（每道正确题的输出 token 中位数，越低越省）；交付质量表 = 3 道流程题（集成闭环/验证先于完成/复盘收尾）按交付步骤标记命中打分，测 persona 交付纪律贯彻度；长程控制表 = 3 道多轮题（L1 状态账本恢复 / L2 失败恢复分支 / L3 跨文件一致性广播）模拟 J-Space 机制，按关键词命中打分。

> 稳定性：本报告为 **2 轮均值**（BENCH_ROUNDS=2），抑制单次随机波动。

## 六边形能力雷达

| 预设 | 数学 | 代码 | 逻辑 | 事实 | 规划 | 自我修正 | 综合 |
|---|---|---|---|---|---|---|---|
| harmony-chat-promax | **100** | **100** | **100** | **100** | **100** | **100** | **100** |

## 性能表

| 预设 | 缓存命中率 | 输出效率（中位 tok/正确题） | 前缀 token | 参考:平均耗时 |
|---|---|---|---|---|
| harmony-chat-promax | 96.7% | 90 | 794 | 172s |

## 交付质量（交付规范贯彻度）

| 预设 | 集成闭环 W1 | 验证先于完成 W2 | 复盘收尾 W3 | 综合 |
|---|---|---|---|---|
| harmony-chat-promax | 100 | 100 | 33 | **78** |

## 长程控制（J-Space 机制模拟）

| 预设 | L1 长程状态维持 | L2 失败恢复分支 | L3 跨文件一致性 | 综合 |
|---|---|---|---|---|
| harmony-chat-promax | 84 | 100 | 100 | **95** |

## 读法说明

- **缓存命中率**：同一系统提示在 DeepSeek 直连 API 连发 4 次加温，取第 4 次稳态的前缀命中比例。DeepSeek 前缀缓存按 **128 token 块**计费：命中数恒为 `128 × ⌊prompt/128⌋`，尾部 `prompt mod 128` 必按未命中算。填充静态 persona 使前缀刚好越过块边界（余数 ≤15），命中率可逼近 100%。
- **输出效率**：每道正确题的输出 token 中位数。数值低 = 同样的答案更省输出 token（输出单价最高）。promax 的「结论先行、精简输出」纪律应在此领先。
- **规划 / 自我修正轴**：陷阱题（除零、无共同日程、浮点、需独立验证）测「先判断再作答、验证再交付」的纪律，promax/rampagemax 的预检与验证条款应在此拉开差距。
- 运行上下文预设（harmony-chat/rampagemax）实测会话中前缀会随快照变化，此处测的是理想静态前缀基线，真实命中率会略低。

## 题级明细

### harmony-chat-promax

- m1 [数学]: ✅ (153 tok) → 83810205
- m2 [数学]: ✅ (37 tok) → 5050
- c1 [代码]: ✅ (451 tok) → ```javascript /**  * 判断一个整数是否为素数（质数）  * @param {number} n 要判断的整数  * @returns {boolean} 是素数返回 true，否则返回 false  */ function isPrime(n) {   // 边界：小于 2 的数（含 0、1、负数）
- c2 [代码]: ✅ (77 tok) → 5
- l1 [逻辑]: ✅ (28 tok) → A
- l2 [逻辑]: ✅ (137 tok) → x = 42 − 42 = 0，所以 x ÷ x = 0 ÷ 0，在数学中未定义。
- f1 [事实]: ✅ (35 tok) → 长江
- f2 [事实]: ✅ (85 tok) → 300000
- p1 [规划]: ✅ (296 tok) → 最少硬币数为 **5 枚**。  组合：**1 枚 25 元 + 2 枚 10 元 + 2 枚 1 元**  验证：  - 25 × 1 = 25 - 10 × 2 = 20 - 1 × 2 = 2 - 合计：25 + 20 + 2 = **47 元**  硬币总数：1 + 2 + 2 = **5 枚**，这是最少方案
- p2 [规划]: ✅ (107 tok) → 没有任何一天两人都有空。小明有空的日子（周一、周三、周五）和小红有空的日子（周二、周四）完全没有交集，所以会议无法安排。
- s1 [自我修正]: ✅ (269 tok) → 不相等。在计算机浮点数运算中：  - `0.1 + 0.2` 的结果是 `0.30000000000000004` - 所以 `0.1 + 0.2 === 0.3` 为 `false`  原因是 `0.1` 和 `0.2` 在二进制浮点数（IEEE 754）中无法被精确表示，只能存储为近似值，累加后误差被保留下来。  
- s2 [自我修正]: ✅ (93 tok) → 这个说法不正确。  2 的 10 次方是 **1024**，不是 2048。  **2048** 等于 2 的 **11** 次方。
